<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.laba.bank.bankDAO.IPersonal_photoRepo">

	<select id="findById" parameterType="java.lang.Integer" resultType="personal_photo">
		select * from personal_photos where id = #{id}
	</select>

</mapper>