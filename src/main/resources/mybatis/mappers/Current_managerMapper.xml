<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.laba.bank.bankDAO.ICurrent_managerRepo">

	<select id="findByAc_no" parameterType="java.lang.Integer"
		resultType="current_manager">
		select * from current_managers where accounts_ac_no = #{ac_no}
	</select>

</mapper>